Pod::Spec.new do |s|
  s.name         = 'Silly-Transitions'
  s.version      = '0.0.1'
  s.summary      = 'Custom CAAnimation transitions for iOS 7.0'
  s.author = {
    'Brian nickel' => 'bnickel@stackoverflow.com'
  }
  s.source = {
    :git => 'https://github.com/bnickel/Silly-Transitions',
    :tag => ''
  }
  s.source_files = 'Silly Transitions/Source/*.{h,m}'
end